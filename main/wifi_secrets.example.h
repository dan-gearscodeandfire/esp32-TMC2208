#pragma once
// Template. Copy this file to wifi_secrets.h and fill in your network.
// wifi_secrets.h is gitignored so real credentials are never committed.
#define WIFI_SSID "your-ssid"
#define WIFI_PASS "your-password"
